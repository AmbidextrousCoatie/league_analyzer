"""
Input adapters: load rows from files, APIs, etc.

Each source is independent. Adapters output dict rows in the canonical column names
expected by `database.conversion.bowlingbayern_legacy_core` (see INPUT_TO_LEGACY_CSV_MAPPING.md).
"""
